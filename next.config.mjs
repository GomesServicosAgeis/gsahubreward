// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remova completamente a linha do turbopack
  // Configurações válidas para Next.js 14:
  reactStrictMode: true,
}

export default nextConfig